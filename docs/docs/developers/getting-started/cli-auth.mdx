---
title: CLI Auth
description: Install the Openlane CLI and authenticate for local development workflows
sidebar_position: 4
tags:
    - cli
    - api
    - development
    - automation
keywords:
  - openlane cli
  - command line interface
---

import Steps from '@site/src/components/Steps';

# Installing the Openlane CLI

The Openlane CLI authenticates for local development either with an API or personal access token or with username and password credentials.

## Installation

### MacOS

The Openlane CLI is available for MacOS using `homebrew`.

```shell
brew install theopenlane/tap/openlane
```


### From Source

Download the CLI directly from [GitHub releases](https://github.com/theopenlane/core/releases).



## Authentication

:::info
OAuth login is not currently supported using the CLI. You must use an [API or Personal Access Token](../security/api-tokens-and-pats.mdx) instead.
:::


### Using API or Personal Access Tokens

```shell
export CORE_TOKEN=tola_REDACTED
```

```shell
export CORE_PAT=tolp_REDACTED
```

### Using username and password credentials

<Steps>

1. Run the `login` command

    ```shell
    openlane login -u meow@example.com
    ```

1. You should see a prompt to enter your password
1. You should see a prompt to store your access token, refresh token, and session token in your local keychain

</Steps>

:::tip
We recommend setting the Openlane CLI to always allow in your Keychain so you do not need to continuously approve the storage and retrieval on every command.
:::
